import pytest
import itertools
from intlabeler.streams import helper

@pytest.mark.usefixtures("input_streams")
def test_roundrobin(input_streams):
    for item in helper.read_iterables(input_streams):
        print(item)
